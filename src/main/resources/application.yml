spring:
  application:
    name: eventosapi
  datasource:
    url: jdbc:postgresql://localhost:5432/evento
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update